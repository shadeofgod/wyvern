{
  "author": "shadeofgod",
  "name": "wyvern",
  "version": "1.0.0",
  "description": "A light weight bearychat application written with Typescript and Electron",
  "scripts": {
    "build": "tsc",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "start": "npm run build && ELECTRON_ENV=development electron ./dist/main.js"
  },
  "keywords": [
    "Bearychat",
    "Electron",
    "typescript"
  ],
  "dependencies": {
    "axios": "^0.17.1"
  },
  "devDependencies": {
    "electron": "^1.8.2",
    "tslint": "^5.9.1",
    "typescript": "^2.6.2"
  }
}
